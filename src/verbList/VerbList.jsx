import classes from './verbList.module.css';

const VerbList = () => {
    return(
        <div className={classes.verbLst}>
            <h1 className={classes.header}>Verb List</h1>
            <div className={classes.verbContainer}>
                {/* <VerbWordElem/> */}
            </div>
        </div>
    );
};

export default VerbList;